#include <bits/stdc++.h>
using namespace std;

int n;
vector<int> adj[64];

int main(){
	cin.sync_with_stdio(0); cin.tie(0);
	cin >> n;
	for (int i = 0; i < n; ++i){
		ll a; cin >> a;
		
	}
return 0;}
