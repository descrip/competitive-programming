#include <bits/stdc++.h>
using namespace std;

int N, M, A[100005], D[100005];

bool gao(int k) {

}

int main() {
	ios_base::sync_with_stdio(0);
	cin.tie(0);
    cin >> N >> M;
    for (int i = 0; i < N; ++i)
        cin >> D[i];
    for (int i = 0; i < M; ++i)
        cin >> A[i];
	return 0;
}

