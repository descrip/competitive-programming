//what i've found:
//try all
//bin search for possible set ends
//use them all and see if that t is valid
#include <iostream>
using namespace std;

const int MAXN = 1e+5;
int n, a[MAXN], gsum, psum;

int main(){
	cin >> n;
	for (int i = 0; i < n; i++)
	return 0;
}
