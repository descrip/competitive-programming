#include <vector>
#include <iostream>
#include <cstdio>
using namespace std;

#define pb push_back
#define gcd __gcd
typedef long long ll;

int main(){
	int n;
	scanf("%d",&n);
	vector<int> a (n);
	for (int i = 0; i < n; ++i)
		scanf("%d",&a[i]);
	vector<int> b = a;
	for (int i = 0; i < n; ++i){
		
	}
	printf("%d\n",1);
}
