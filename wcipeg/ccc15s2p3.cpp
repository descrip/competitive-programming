#include <bits/stdc++.h>
using namespace std;

#define first fi
#define second se

int main(){
	ios_base::sync_with_stdio(0); cin.tie(0);
	int X, K, N, Q; cin >> X >> K >> N >> Q;
	vector<pair<pair<int, int>, int>> lines (N);
	for (int i = 0; i < N; ++i)
		cin >> lines[i].fi.fi >> lines[i].fi.se >> lines[i].se;
	static vector<pair<double, int>> act[2000], ser[2000];
	for (int i = 0; i < N; ++i)
		for (int j = 0; j < N; ++j)

return 0;}

