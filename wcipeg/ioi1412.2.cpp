#include <iostream>
using namespace std;

int N, K;
pair<int, int> tree[3*2000000], lazy[3*2000000];

void upd(int n, int l, int r, int ql, int qr, int u, int op) {
  if (r < ql || r < ql || l > r)
    return;
}

int main() {
  ios_base::sync_with_stdio(0); cin.tie(0);
  cin >> N >> K;
return 0;}

