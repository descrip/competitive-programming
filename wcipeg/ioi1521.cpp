#include <bits/stdc++.h>
using namespace std;

int main(){
	ios_base::sync_with_stdio(0); cin.tie(0);
	int N; cin >> N;
	vector<int> X(N), Y(N);
	for (int i = 0; i < N; ++i)
		cin >> X[i];
	for (int i = 0; i < N; ++i)
		cin >> Y[i];
	long long T = 1;
return 0;}

