#include <bits/stdc++.h>
using namespace std;

int main(){
	ios_base::sync_with_stdio(0); cin.tie(0);
	int r, c; cin >> r >> c;
	static int a[751][751];
	for (int y = 0; y < r; ++y)
		for (int x = 0; x < c; ++x)
			cin >> a[y][x];
	
return 0;}
