#include <bits/stdc++.h>
using namespace std;

const int NMAX = 4e4+5, SIZE = (1<<((int)ceil(log2(NMAX))+1))+5;
int n, d;
int l0[SIZE], r0[SIZE], l1[SIZE], r1[SIZE];

int main(){
	ios_base::sync_with_stdio(0); cin.tie(0);
return 0;}
