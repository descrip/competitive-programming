#include <bits/stdc++.h>
using namespace std;

int N, Q, A[7][7], R[7];
bool row[7], col[7];

int recur(

int main(){
	ios_base::sync_with_stdio(0); cin.tie(0);
	cin >> N;
	for (int y = 0; y < N; ++y)
		for (int x = 0; x < N; ++x)
			cin >> A[y][x];
	cin >> Q;
	for (int i = 0; i < Q; ++i)
		cin >> R[i];
return 0;}

